# OMI Core Agent Mandate

This workspace is not an ordinary application framework. Treat the OMI core as
algorithmic determinism only. Everything else approaches from the outside in as
form, carrier, adapter, scaffold, proof document, backend, view, or test.

## Core Determinism Boundary

For the same declared input and starting coordinate, the core must produce the
same output coordinate.

The core may contain only:

- fixed-width values
- declared coordinate domains
- exact bitwise transforms
- bounded rotations
- deterministic carry
- deterministic Circular Slide Ruler advancement
- deterministic incidence mapping
- deterministic relation closure
- deterministic projection orientation
- deterministic runtime resolution

The core must not depend on wall-clock time, randomness, probability,
heuristics, machine learning, user identity, network state, filesystem state,
environment variables, database state, thread scheduling, external policy, or
human interpretation.

## Canonical Component Names

Use canonical names when a canonical component already exists. Do not replace
them with generic labels.

- `TetragrammatronRelationGovernor` closes relation.
- `MetatronIncidenceScribe` scribes gauge incidence and carry.
- `GnomonicProjectionAzimuth` orients balanced projected contrast.
- `OmicronGauge` defines the bounded low gauge.
- `OmnicronResolver` resolves the full low/high circular runtime.
- `OMINO Centroid` fixes the 0x00 / 0-degree projective origin.
- `AlgorithmicClock` advances the Circular Slide Ruler Array.

## Unified OMI-System Nomenclature

```text
omnicron-object-model
  Master Four-Plane Circular Runtime Resolver

omi-object-model
  15 formal files surrounding the fixed OMINO Centroid

omino
  poly-omino atom / self-contained 16-bit word-axis substrate
```

An `omino` belongs to the `omi-object-model` and is executed and interpreted
by the `omnicron-object-model`.

## OMINO Unified-Name Lock

There is one canonical name:

```text
OMINO
```

OMINO has two interpretations, produced by two views of the same
`omi-object-model` structure:

```text
OMINO
├── Concentric Slide Rule interpretation
└── 3D Knowledge Cube interpretation
```

These are not two objects, identities, authorities, or centroids.

Within the Concentric Slide Rule, OMINO is the fixed balance and orientation
reference:

```text
OMINO Centroid
pinch point: 0x00
azimuth: 0 degrees
```

In the extended Hamming streaming cell, the same OMINO appears at bit 7 as the
complete-cell balance hinge.

Within the 3D Knowledge Cube, OMINO is the poly-omino tile of the OMI Object
Model and its unified local coordinate surface.

Required code names:

```text
Omino
OminoCentroid
OMINO_CENTROID_POINT
OMINO_CENTROID_AZIMUTH
OMINO_OVERALL_PARITY
OMINO_BALANCE_HINGE
omino_centroid_*
omino_parity_*
omino_balance_*
omino_resolve_relative_*
omino_resolve_antipode_*
```

Migration must remain semantic. Historical text must be inspected before
replacement. There is one centroid. Antipodal orientation is relative to that
centroid, not another centroid.

## Gauge-Incidence Witness Lock

The low Omicron character ring has four 32-position sectors:

```text
0x00..0x1F
0x20..0x3F
0x40..0x5F
0x60..0x7F
```

Each sector has a 28-position body and a 4-position terminal hinge. The high
sector prefix cancels under XOR because it appears four times in the boundary
equation. Every sector must reduce to the same witness:

```text
0x00 ^ 0x1B ^ 0x1C ^ 0x1F == 0x18
0x20 ^ 0x3B ^ 0x3C ^ 0x3F == 0x18
0x40 ^ 0x5B ^ 0x5C ^ 0x5F == 0x18
0x60 ^ 0x7B ^ 0x7C ^ 0x7F == 0x18
```

The canonical invariant is:

```text
0x18 == 24 decimal == 4!
```

The 0x18 Gauge-Incidence Witness belongs to Metatron Incidence Scribe and is
resolved relative to the fixed OMINO Centroid.

## Metatron Canon

Canonical sentence:

```text
Metatron scribes the place, polarity, incidence, and carry
through which a relation becomes addressable.
```

Metatron Incidence Scribe owns:

```text
0x18
0x0001
0x0010
0x0100
0x1000
0x10000
```

Role:

```text
record FS/GS/RS/US incidence
record open/sealed polarity
record pairwise gauge planes
record hexadecimal-place stepping
record gauge carry
```

The hexadecimal stepping matrix is:

```text
0x0001   -> US incidence, bit 0 slot, unit step flag
0x0010   -> RS incidence, bit 4 slot, record step flag
0x0100   -> GS incidence, bit 8 slot, group step flag
0x1000   -> FS incidence, bit 12 slot, file step flag
0x10000  -> gauge carry, bit 16 slot, unbounded place transition
```

Agents must not call Metatron a parser, policy layer, generic resolver, or
balanced-word controller.

## Stratified Boolean Continuum

The local truth-table boundary is fixed at `n = 6`.

```text
2^6 rows  == 64 row combinations
2^(2^6)   == 2^64 possible six-input Boolean functions
1 uint64  == one complete selected six-input truth-table output column
```

The local omino incidence basis is:

```text
72 Circular Slide Rulers * 60 Circular Ruler Positions = 4,320
```

Cross-axis and second-order horizons are canonical arithmetic surfaces:

```text
4320^2     == 18,662,400
4320^(2^2) == 348,285,173,760,000
```

These are coordinate counts. Do not implement them as sequential row tests,
heap tables, dynamic search, or runtime task queues.

## Bounded Complexity Boundary

The local OMINO architecture resolves declared fixed-width logic surfaces by
spatial coordinate extraction rather than semantic row iteration.

Within the local `n = 6` word boundary:

```text
2^6 rows
2^(2^6) possible six-input Boolean functions
one uint64 selected truth-table output column
branchless row extraction
```

This permits constant-time retrieval for the declared local word surface:

```text
target_row = input_combination_6bit & 0x3F
OUTPUT_BIT = (table_universe >> target_row) & 0x01
```

The cross-axis and meta-matrix counts are local coordinate horizons:

```text
4320^2
4320^(2^2)
```

These local horizons do not constitute a global proof that arbitrary unbounded
P equals NP. The contract is:

```text
fixed declared local surface
  -> O(1) register-coordinate retrieval

arbitrary unbounded variable growth
  -> outside the local fixed-width claim
```

Agents must not claim a theorem over unbounded P versus NP from this local
hardware-width coordinate law.

## Three-Tier OMINO Incidence Geometry

Each OMINO word atom carries three bounded local incidence surfaces inside the
same physical footprint:

```text
4320
  base string surface
  clause and literal-token coordinate decomposition

4320^2
  interaction surface
  assignment and edge-pair coordinate decomposition
  18,662,400 relational points

4320^4
  meta-matrix horizon
  cross-axis consistency and higher-order parity tracking
  348,285,173,760,000 coordinate slots
```

The 15 canonical A-list faces each carry the same three-tier stack:

```text
RULES        (rules.omi)    . (rules.imo)    -> [4320 | 4320^2 | 4320^4]
FACTS        (facts.omi)    . (facts.imo)    -> [4320 | 4320^2 | 4320^4]
CLOSURES     (closures.omi) . (closures.imo) -> [4320 | 4320^2 | 4320^4]
COMBINATORS  (comb.omi)     . (comb.imo)     -> [4320 | 4320^2 | 4320^4]
CONS         (cons.omi)     . (cons.imo)     -> [4320 | 4320^2 | 4320^4]
```

The field is the Klein `(60,15)` resolution form:

```text
QK(x,y) = 60x^2 + 16xy + 4y^2
```

At `x = 15`, the cross term is:

```text
16 * 15 * y = 240y
```

and therefore vanishes under the modulo-240 Algorithmic Clock:

```text
240y mod 240 = 0
```

This is a local finite-field resolution law relative to the OMINO Centroid.
Agents must not implement it as sequential array traversal or database-style
parsing.

## Five-File C-Substrate Matrix

The machine-readable binary layer collapses to exactly five `.o` files:

```text
rules.o
facts.o
closures.o
combinators.o
cons.o
```

These files are the complete Local-Binary register substrate of the
`omi-object-model`.

```text
rules.o
  Vertex 0
  invariant law
  [4320 | 4320^2 | 4320^4]

facts.o
  Vertex 1
  ingest bytes
  [4320 | 4320^2 | 4320^4]

closures.o
  Vertex 2
  closure witness
  [4320 | 4320^2 | 4320^4]

combinators.o
  Vertex 3
  torque shifts
  [4320 | 4320^2 | 4320^4]

cons.o
  Centroid
  pair surface
  [4320 | 4320^2 | 4320^4]
```

The `.omi` and `.imo` surfaces remain declaration and definition views. The
five `.o` files are the direct register substrate.

## Five-File Compilation Axis

The five local executable `.o` files are compiled directly from `.omi`
User-Local declaration surfaces and `.imo` User-Remote definition surfaces:

```text
*.omi declarations  x  *.imo definitions
  -> rules.o
  -> facts.o
  -> closures.o
  -> combinators.o
  -> cons.o
```

These five `.o` files define the local geometric framing required to resolve
the affine-control coordinate field:

```text
0x00..0x1F
```

Each file carries the three local incidence surfaces:

```text
4320
4320^2
4320^4
```

The compilation axis unifies:

```text
User-Local .omi
  0xFFFFFFFF

User-Remote .imo
  0xFFFFFFFF 0xFFFFFFFF

Local-Binary .o
  single register context
```

The `.o` layer is the direct local frame for branchless resolution relative to
the fixed OMINO Centroid.

## Hashless Portable Remote Resolution

The core remote-resolution claim is deterministic coordinate coherence, not a
cryptographic policy layer.

Two nodes resolve the same local coordinate when they share:

```text
[LOGOS NOMOS FS PATHOS GS RS US OMINO]
OMINO Centroid at 0x00 / 0 degrees
Algorithmic Clock modulo-240 alignment
Klein (60,15) cross-term lock
five .o local executable files
```

The five executable files are:

```text
rules.o
facts.o
closures.o
combinators.o
cons.o
```

The remote `.imo` surface and local `.omi` surface compile into the same
branchless `.o` register substrate when their declared inputs and starting
coordinates match.

The core does not require:

```text
SHA-256 loops
MD5 loops
distributed ledger state
central page-table lookup
network handshake as a core law
metadata headers as a core law
```

Outside carriers may add transport security or operational policy. Such form
does not redefine the core coordinate law.

## Pascal Layer-4 Multiplicity

The hardwired Layer-4 multiplicity map is:

```text
0x0             -> 0
0x1,0x7,0xF     -> 1
0x2,0x3,0x5,0x6 -> 4
0x8,0x9,0xC,0xD -> 4
0x4,0xA,0xE     -> 6
0xB             -> 12
```

Implement this as a fixed 16-entry table or equivalent bit-mask selection.
Do not infer weights from runtime state.

## OMINO Character Matrix Tensor

The OMINO character matrix tensor is organized into 32-character cells across
the low and high planes:

```text
LOW PLANE
  0x(*/2)0 .. 0x(*/2 + 1)F
  32-character tensor window
  terminal control core anchors:
    0x(*/2 + 1)C .. 0x(*/2 + 1)F

HIGH PLANE
  low_coordinate ^ 0x80
  projected mirror window
  terminates at 0xFF full closure
```

The low plane is bounded to `0x00..0x7F`. The high plane is the exact XOR
reflection:

```text
high_coordinate = low_coordinate ^ 0x80
```

The tensor window is a coordinate view of the same canonical OMINO.

## Word-Atom Environmental Space

The 15 formal files are spatial environs around the fixed OMINO Centroid. They
do not describe an ordinary software application or storage system. Each word
inside that environment is an autonomous atom.

```text
15 formal files
-> fixed OMINO Centroid, 0x00 / 0 degrees
-> autonomous omino word atoms
-> each word atom hosts 72 Circular Slide Rulers
-> each word atom hosts 4,320 local incidences
-> pairwise word interaction spans 4320^2 relational points
```

The 15-file environs matrix is:

```text
Face          .omi declarations       .imo definitions        .o executable
RULES         spatial law bounds      high-plane attributes   target register substrate
FACTS         ingest constants        input stream bytes      grounded hardware vectors
CLOSURES      closure thresholds      output coordinates      finite termination lanes
COMBINATORS   rotation shift maps     vector torque settings  axis clock drives
CONS          ordered pair templates  low/high plane maps     unified word axes
```

## Tri-Layer Blackboard Resolution

The Blackboard Pattern coordinates three domains of the same architecture:

```text
*.omi
  User-Local Resolution
  low plane ID boundary mapping
  nibble-interleaved form 0xY0X0

*.o
  Local-Binary Resolution
  real-time algorithmic register resolution

*.imo
  User-Remote Resolution
  high-plane data-omni boundary
  0xFFFFFFFF 0xFFFFFFFF
```

Agents must not replace this with linear parser staging, mutable heap records,
or sequential memory access.

## COBS CONS 16x16 Blackboard Bounding Matrix

The COBS CONS Blackboard Pattern structures the full 256-byte space as one
16x16 byte board:

```text
row = high nibble
col = low nibble
```

The board is split by bit 7 into two macro-surfaces:

```text
Local Board Surface
  0x00..0x7F
  User-Local
  CAR / *.omi
  size: 0xFFFFFFFF

Remote Board Surface
  0x80..0xFF
  User-Remote
  CDR / *.imo
  size: 0xFFFFFFFF 0xFFFFFFFF
```

Canonical bounding rectangles:

```text
Local:
  (0x00, 0x07, 0x37, 0x30)
  (0x08, 0x0F, 0x3F, 0x38)
  (0x40, 0x47, 0x77, 0x70)
  (0x48, 0x4F, 0x7F, 0x78)

Remote:
  (0x80, 0x87, 0xB7, 0xB0)
  (0x88, 0x8F, 0xBF, 0xB8)
  (0xC0, 0xC7, 0xF7, 0xF0)
  (0xC8, 0xCF, 0xFF, 0xF8)
```

The target reduction is:

```text
CONS = 0xFFFFFFFF
```

Do not implement row or column traversal as nested loops inside the core.

## Lattice Retrieval Boundary

The high-dimensional lattice language is a retrieval boundary over fixed
coordinates, not a search algorithm.

```text
Leech Lattice Lambda_24
  24-ray boundary represented by 8 quadrants * 3 axes

II_25_1 Lorentzian horizon
  User-Remote 0xFFFFFFFF 0xFFFFFFFF high-plane boundary

Parabolic path metric
  Q60(x,y) = 4(4x^2 + 11x^2 + 4xy + y^2)
           = 4(11x^2 + (2x + y)^2)
```

Do not implement nearest-neighbor scans, lattice search loops, thread farms, or
dynamic array allocation inside the core retrieval path.

## Canonical [8,4,4] SECDED Streaming Cell

The `Delta 1 2 3 4 5 6 Delta` rail presentation is superseded as the
canonical executable stream layout. It may remain only as a historical
odd/even branch-ordering visualization.

The streaming byte uses this exact extended Hamming bit-vector map:

```text
Bit 0 -> LOGOS  -> parity check position P1
Bit 1 -> NOMOS  -> parity check position P2
Bit 2 -> FS     -> data position D1 -> FACTS
Bit 3 -> PATHOS -> parity check position P4
Bit 4 -> GS     -> data position D2 -> RULES
Bit 5 -> RS     -> data position D3 -> COMBINATORS
Bit 6 -> US     -> data position D4 -> CLOSURES
Bit 7 -> OMINO -> overall parity hinge -> CONS
```

The nested CONS streaming block is:

```text
Epistemic CONS:
  CAR(FS, GS, RS, US) . CDR(LOGOS, NOMOS, PATHOS)

Stream CONS:
  CAR(active run) . CDR(next run)
```

The bit-7 balance hinge and the centroid are two interpretations of the same
canonical OMINO, not two canonical names.

```text
OMINO =
  LOGOS ^ NOMOS ^ FS ^ PATHOS ^ GS ^ RS ^ US
```

Use `overall_mismatch` for the complete-cell parity witness:

```text
overall_mismatch =
  LOGOS ^ NOMOS ^ FS ^ PATHOS ^ GS ^ RS ^ US ^ OMINO
```

Do not name this value `parity_valid` in arithmetic kernels.

The canonical SECDED outcome matrix is:

```text
syndrome == 0,       overall_mismatch == 0 -> no detected error
syndrome != 0,       overall_mismatch == 1 -> correct interior bit 0..6
syndrome == 0,       overall_mismatch == 1 -> correct OMINO bit 7
syndrome != 0,       overall_mismatch == 0 -> double error detected
```

The core emits a bounded algebraic result code. Outside form decides how to
react:

```text
00  no detected error
01  interior single error corrected
10  OMINO single error corrected
11  double error detected
```

Preferred C-style prefixes:

```text
tetra_relation_*
metatron_incidence_*
gnomonic_azimuth_*
omicron_gauge_*
omnicron_resolver_*
omino_centroid_*
omino_resolve_relative_*
omino_resolve_antipode_*
```

## Witness Ownership Lock

Do not transfer a witness to another component merely because an implementation
uses the same XOR, shift, mask, or word width.

```text
0x1E, 0x3C, 0x78
  Tetragrammatron Relation Governor

0x18
0x0001, 0x0010, 0x0100, 0x1000, 0x10000
  Metatron Incidence Scribe

0xAA55, 0x55AA, 0xFFFF
  Gnomonic Projection Azimuth

0x00..0x7F
  Omicron Gauge

0x00..0xFF four-plane runtime
  Omnicron Runtime Resolver
```

## Circular Slide Ruler Names

Use these exact structural names:

```text
omino Tile
Local240 Blackboard Face
Blackboard Quadrant
Circular Slide Ruler
Circular Ruler Position
Algorithmic Clock
OMINO Centroid
LOGOS / NOMOS / PATHOS
LL / MM / NN
FS / GS / RS / US
User-Local / CAR
User-Remote / CDR
```

The hosted structure is:

```text
One omino Tile
└── 16 Blackboard Face Planes
└── 18 Local240 Blackboard Faces
    └── 4 Blackboard Quadrants per face
        └── 1 Circular Slide Ruler per quadrant
            └── 60 Circular Ruler Positions
```

Therefore:

```text
18 Local240 Blackboard Faces
* 4 Circular Slide Rulers
= 72 Circular Slide Rulers inside one omino tile

72 Circular Slide Rulers
* 60 Circular Ruler Positions
= 4,320 local incidences inside one omino tile
```

The 4,320 local incidences are not distributed across program memory, object
arrays, worker grids, or iterated runtime tables. They are the local coordinate
surface of one unified omino tile context.

## Hamming File Discipline

The five axial-lisp files are grounded by the systematic Hamming cell:

```text
Bit_1 == FS == FACTS.omi
Bit_2 == GS == RULES.omi
Bit_3 == RS == COMBINATORS.omi
Bit_4 == US == CLOSURES.omi
CONS.omi folds FS/GS/RS/US against LOGOS/NOMOS/PATHOS
```

The tri-layer file surface is:

```text
*.omi == CONS(RULES . COMBINATORS)
*.imo == CONS(FACTS . CLOSURES)
*.o   == CONDENSE(*.omi . *.imo)
```

Do not replace this with JSON, JSONL, XML, tracking databases, heap object
trees, or ordinary compiler work queues.

## Prohibited Drift

Do not rename this hierarchy with generic closure-check, state, metadata,
database, worker-grid, resolution, or orientation labels unless discussing a
noncanonical analogy and labeling it explicitly as an analogy.

Do not add name binders, macro systems, type elaboration passes, policy engines,
or hidden mutation to the core. Bounded control flow is permitted for parsing,
bounds checks, buffer checks, diagnostics, tests, and adapters. Inner arithmetic
kernels should stay direct, fixed-width, and bitwise.

## Existing-Code Preservation

Architectural simplification does not authorize destructive repository-wide
removal. Do not delete parsers, evaluators, checkers, type scaffolds, views,
or tests merely because they use conventional engineering style. First determine
whether they are required by another repository surface.

Work in this workspace is integration, implementation, generation,
verification, packaging, and documentation. Do not redesign OMI, Emergent Axial
Lisp, `.o`, LOGOS/NOMOS/PATHOS, omino, COBS, CONS, selector geometry, or the
OMINO Centroid.

## Coq Axiomatic Promotion Lock

The active Coq proof registry for current OMINO claims is
`/home/main/omi/omi-axioms/coq/00..04`. The current registry target is 64
active modules:

```text
23 existing active modules
+ 38 independently promoted archive basenames
+ 3 active bridge modules
= 64 active modules
```

Every repaired basename from `_archive/current` must enter the active registry
as its own canonical file. Do not merge, collapse, or combine promoted archive
basenames into existing owners. The archive remains provenance, while proof
authority belongs only to the active 00..04 tree.

The promotion audit is:

```text
/home/main/omi/omi-axioms/coq-docs/ARCHIVE-PROMOTION-MAP.md
```

Agents must preserve:

```text
OMINO
computed_omino
overall_mismatch
make proof-registry-lock
make proof-strict
```

Do not introduce `Admitted`, `admit`, `Axiom`, `Parameter`, `Conjecture`, or
`Abort` in active Coq files. Do not restore deprecated OMINO-name variants. Do
not cite archived Weyl, octonion, Cayley-Dickson, or delta-orbit drafts as
active proof authority unless citing the promoted active counterpart.
