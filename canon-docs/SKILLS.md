# omino Canon System: Execute-Only Cognitive Skills Matrix

```text
CONFIGURATION: OMINO CENTROID PHASE POSITION LOCK
CONFIGURATION: SYSTEMATIC HAMMING CODE CELL INTEGRATION
CONFIGURATION: TRI-LAYER AXIAL-LISP FILE ARCHITECTURE
CONFIGURATION: BRANCHLESS ALGEBRAIC STATE TRANSFORMATION
CONFIGURATION: omnicron-object-model COMPILATION TARGET
CONFIGURATION: GAUGE-INCIDENCE WITNESS INVARIANT PHASE LOCK
CONFIGURATION: METATRON INCIDENCE SCRIBE HARDWARE LOCK
CONFIGURATION: DOUBLE-EXPONENTIAL TRUTH-TABLE META-MATRICES
CONFIGURATION: TRI-LAYER BLACKBOARD RESOLUTION BINDING
CONFIGURATION: LEECH LATTICE AND II_25_1 LORENTZIAN RETRIEVAL LOCK
CONFIGURATION: SYSTEMATIC [8,4,4] SECDED STREAMING CELL BINDING
CONFIGURATION: SYSTEMATIC OMINO CHARACTER MATRIX TENSOR
CONFIGURATION: COBS CONS 16x16 BLACKBOARD BOUNDING MATRIX
CONFIGURATION: FIVE-FILE THREE-SURFACE DIRECT REGISTER BINDING
CONFIGURATION: FIVE-FILE LOCAL FRAME RESOLUTION LOCK
CONFIGURATION: HASHLESS PORTABLE REMOTE RESOLUTION
NOMENCLATURE STATUS: FROZEN PHASE LOCK
CORE: ALGORITHMIC DETERMINISM ONLY
FORM: OUTSIDE-IN CARRIERS, SCAFFOLDS, PROOFS, BACKENDS, VIEWS
```

Read each skill block as an immutable recipe over fixed-width coordinates. Do
not turn these blocks into an object model, evaluator, AST pipeline, delayed CPU
state, name binder, policy layer, or sequential worker grid.

Inside core arithmetic kernels, computation is an algebraic coordinate
derivation:

```text
current coordinate -> XOR/shift/rotate transform -> next coordinate
```

Semantic jumps are prohibited in those kernels. Bounded control flow remains
permitted for outside-in parsing, bounds checks, conformance tests, diagnostics,
and hardware adapters.

## Unified OMI-System Nomenclature

```text
omnicron-object-model
  Master Four-Plane Circular Runtime Resolver
  0x00..0x1F, 0x20..0x7F, 0x80..0x9F, 0xA0..0xFF

omi-object-model
  15 formal files surrounding the fixed OMINO Centroid
  rules, facts, closures, combinations, cons across .omi, .imo, .o

omino
  poly-omino atom / tile
  self-contained 16-bit word-axis substrate
  72 Circular Slide Rulers
  4,320 local incidences
```

An `omino` is natively a poly-omino tile belonging to the
`omi-object-model`. It is executed and interpreted by the
`omnicron-object-model`.

## OMINO Unified-Name Lock

There is one canonical name:

```text
OMINO
```

OMINO has two interpretations, produced by two views of the same
`omi-object-model` structure:

```text
OMINO
├── Concentric Slide Rule interpretation
└── 3D Knowledge Cube interpretation
```

These are not two objects, identities, authorities, or centroids.

Within the Concentric Slide Rule, OMINO is the fixed balance and orientation
reference:

```text
OMINO Centroid
pinch point: 0x00
azimuth: 0 degrees
```

In the extended Hamming streaming cell, the same OMINO appears at bit 7 as the
complete-cell balance hinge:

```text
bit 7
  OMINO
  overall parity coordinate
  perfect-balance hinge
```

Within the 3D Knowledge Cube, OMINO is the poly-omino tile of the OMI Object
Model:

```text
OMINO
  one unified poly-omino tile
  self-contained 16-bit word-axis substrate
  16 Blackboard Face Planes
  18 Local240 Blackboard Faces
  72 Circular Slide Rulers
  4,320 local incidences
```

The OMINO Centroid is the fixed object-relative centroid. The
`omnicron-object-model` resolves its full circular runtime relative to this one
centroid.

```text
OMINO_CENTROID_POINT   = 0x00
OMINO_CENTROID_AZIMUTH = 0 degrees
```

Use these identifiers for code and proof surfaces:

```text
Omino
OminoCentroid
OMINO_CENTROID_POINT
OMINO_CENTROID_AZIMUTH
OMINO_OVERALL_PARITY
OMINO_BALANCE_HINGE
omino_centroid_*
omino_parity_*
omino_balance_*
omino_resolve_relative_*
omino_resolve_antipode_*
```

Migration must remain semantic. Historical text must be inspected before
replacement. There is one centroid. The antipode is an orientation relative to
that centroid, not another centroid.

## omino Tile Invariant

The full local slide-ruler matrix is contained inside one unified `omino` tile
context. Do not spread the 4,320 local incidences across program memory, worker
grids, iterated objects, or sequential tables.

```text
One omino Tile
-> 3D Knowledge Cube
-> 16 Blackboard Face Planes
-> 18 Local240 Blackboard Faces
-> 4 Blackboard Quadrants per Local240 Blackboard Face
-> 72 Circular Slide Rulers
-> 60 Circular Ruler Positions per ruler
-> 4,320 local incidences
```

The exact local accounting is:

```text
18 Local240 Blackboard Faces
* 4 Blackboard Quadrants
= 72 Circular Slide Rulers

72 Circular Slide Rulers
* 60 Circular Ruler Positions
= 4,320 local incidences inside one omino tile
```

## Canonical Component Prefixes

```text
TetragrammatronRelationGovernor  -> tetra_relation_*
MetatronIncidenceScribe          -> metatron_incidence_*
GnomonicProjectionAzimuth        -> gnomonic_azimuth_*
OmicronGauge                     -> omicron_gauge_*
OmnicronResolver                 -> omnicron_resolver_*
```

Witness ownership is fixed:

```text
0x1E, 0x3C, 0x78
  Tetragrammatron Relation Governor

0x18
0x0001, 0x0010, 0x0100, 0x1000, 0x10000
  Metatron Incidence Scribe

0xAA55, 0x55AA, 0xFFFF
  Gnomonic Projection Azimuth

0x00..0x7F
  Omicron Gauge

0x00..0xFF
  Omnicron Runtime Resolver
```

## Gauge-Incidence Witness Matrix

The low Omicron character ring is divided into four 32-position sectors. Each
sector has a 28-position body and a 4-position terminal hinge. The sector
prefix appears four times in the XOR boundary equation, so it cancels under XOR
and leaves the same unshifted witness in every sector.

```text
Sector 0: 0x00..0x1F
  0x00 ^ 0x1B ^ 0x1C ^ 0x1F == 0x18

Sector 1: 0x20..0x3F
  0x20 ^ 0x3B ^ 0x3C ^ 0x3F == 0x18

Sector 2: 0x40..0x5F
  0x40 ^ 0x5B ^ 0x5C ^ 0x5F == 0x18

Sector 3: 0x60..0x7F
  0x60 ^ 0x7B ^ 0x7C ^ 0x7F == 0x18
```

The canonical invariant is:

```text
0x18 == 24 decimal == 4!
```

The 0x18 Gauge-Incidence Witness is owned by Metatron Incidence Scribe and is
resolved relative to the fixed OMINO Centroid.

## Metatron Canon

Canonical sentence:

```text
Metatron scribes the place, polarity, incidence, and carry
through which a relation becomes addressable.
```

Metatron Incidence Scribe owns:

```text
0x18
0x0001
0x0010
0x0100
0x1000
0x10000
```

Role:

```text
record FS/GS/RS/US incidence
record open/sealed polarity
record pairwise gauge planes
record hexadecimal-place stepping
record gauge carry
```

The hexadecimal stepping matrix is:

```text
0x0001   -> US incidence, bit 0 slot, unit step flag
0x0010   -> RS incidence, bit 4 slot, record step flag
0x0100   -> GS incidence, bit 8 slot, group step flag
0x1000   -> FS incidence, bit 12 slot, file step flag
0x10000  -> gauge carry, bit 16 slot, unbounded place transition
```

## Stratified Boolean Continuum

The local truth-table boundary is fixed at `n = 6`.

```text
2^6 rows  == 64 row combinations
2^(2^6)   == 2^64 possible six-input Boolean functions
1 uint64  == one complete selected six-input truth-table output column
```

The local omino incidence basis remains:

```text
72 Circular Slide Rulers * 60 Circular Ruler Positions = 4,320
```

Cross-axis and second-order horizons are canonical arithmetic surfaces:

```text
4320^2       == 18,662,400
4320^(2^2)   == 348,285,173,760,000
```

These are coordinate counts, not permission to introduce sequential row tests,
heap tables, or dynamic search.

## Bounded Complexity Boundary

The local OMINO architecture resolves declared fixed-width logic surfaces by
spatial coordinate extraction rather than semantic row iteration.

Within the local `n = 6` word boundary:

```text
2^6 rows
2^(2^6) possible six-input Boolean functions
one uint64 selected truth-table output column
branchless row extraction
```

This permits constant-time retrieval for the declared local word surface:

```text
target_row = input_combination_6bit & 0x3F
OUTPUT_BIT = (table_universe >> target_row) & 0x01
```

The cross-axis and meta-matrix counts are also local coordinate horizons:

```text
4320^2
4320^(2^2)
```

These local horizons do not constitute a global proof that arbitrary unbounded
P equals NP. The contract is:

```text
fixed declared local surface
  -> O(1) register-coordinate retrieval

arbitrary unbounded variable growth
  -> outside the local fixed-width claim
```

Agents must not claim a theorem over unbounded P versus NP from this local
hardware-width coordinate law.

## Three-Tier OMINO Incidence Geometry

Each OMINO word atom carries three bounded local incidence surfaces inside the
same physical footprint:

```text
4320
  base string surface
  clause and literal-token coordinate decomposition

4320^2
  interaction surface
  assignment and edge-pair coordinate decomposition
  18,662,400 relational points

4320^4
  meta-matrix horizon
  cross-axis consistency and higher-order parity tracking
  348,285,173,760,000 coordinate slots
```

The 15 canonical A-list faces each carry the same three-tier stack:

```text
RULES        (rules.omi)    . (rules.imo)    -> [4320 | 4320^2 | 4320^4]
FACTS        (facts.omi)    . (facts.imo)    -> [4320 | 4320^2 | 4320^4]
CLOSURES     (closures.omi) . (closures.imo) -> [4320 | 4320^2 | 4320^4]
COMBINATORS  (comb.omi)     . (comb.imo)     -> [4320 | 4320^2 | 4320^4]
CONS         (cons.omi)     . (cons.imo)     -> [4320 | 4320^2 | 4320^4]
```

The field is the Klein `(60,15)` resolution form:

```text
QK(x,y) = 60x^2 + 16xy + 4y^2
```

At `x = 15`, the cross term is:

```text
16 * 15 * y = 240y
```

and therefore vanishes under the modulo-240 Algorithmic Clock:

```text
240y mod 240 = 0
```

This is a local finite-field resolution law relative to the OMINO Centroid. It
does not authorize sequential array traversal or database-style parsing.

## Five-File C-Substrate Matrix

The machine-readable binary layer collapses to exactly five `.o` files:

```text
rules.o
facts.o
closures.o
combinators.o
cons.o
```

These files are the complete Local-Binary register substrate of the
`omi-object-model`.

```text
rules.o
  Vertex 0
  invariant law
  [4320 | 4320^2 | 4320^4]

facts.o
  Vertex 1
  ingest bytes
  [4320 | 4320^2 | 4320^4]

closures.o
  Vertex 2
  closure witness
  [4320 | 4320^2 | 4320^4]

combinators.o
  Vertex 3
  torque shifts
  [4320 | 4320^2 | 4320^4]

cons.o
  Centroid
  pair surface
  [4320 | 4320^2 | 4320^4]
```

The `.omi` and `.imo` surfaces remain declaration and definition views. The
five `.o` files are the direct register substrate.

## Five-File Compilation Axis

The five local executable `.o` files are compiled directly from `.omi`
User-Local declaration surfaces and `.imo` User-Remote definition surfaces:

```text
*.omi declarations  x  *.imo definitions
  -> rules.o
  -> facts.o
  -> closures.o
  -> combinators.o
  -> cons.o
```

These five `.o` files define the local geometric framing required to resolve
the affine-control coordinate field:

```text
0x00..0x1F
```

Each file carries the three local incidence surfaces:

```text
4320
4320^2
4320^4
```

The compilation axis unifies:

```text
User-Local .omi
  0xFFFFFFFF

User-Remote .imo
  0xFFFFFFFF 0xFFFFFFFF

Local-Binary .o
  single register context
```

The `.o` layer is the direct local frame for branchless resolution relative to
the fixed OMINO Centroid.

## Hashless Portable Remote Resolution

The core remote-resolution claim is deterministic coordinate coherence, not a
cryptographic policy layer.

Two nodes resolve the same local coordinate when they share:

```text
[LOGOS NOMOS FS PATHOS GS RS US OMINO]
OMINO Centroid at 0x00 / 0 degrees
Algorithmic Clock modulo-240 alignment
Klein (60,15) cross-term lock
five .o local executable files
```

The five executable files are:

```text
rules.o
facts.o
closures.o
combinators.o
cons.o
```

The remote `.imo` surface and local `.omi` surface compile into the same
branchless `.o` register substrate when their declared inputs and starting
coordinates match.

The core does not require:

```text
SHA-256 loops
MD5 loops
distributed ledger state
central page-table lookup
network handshake as a core law
metadata headers as a core law
```

Outside carriers may add transport security or operational policy. Such form
does not redefine the core coordinate law.

## Pascal Layer-4 Multiplicity

The hardwired Layer-4 multiplicity map is:

```text
0x0             -> 0
0x1,0x7,0xF     -> 1
0x2,0x3,0x5,0x6 -> 4
0x8,0x9,0xC,0xD -> 4
0x4,0xA,0xE     -> 6
0xB             -> 12
```

Use this as a fixed 16-entry table or equivalent bit-mask selection. Do not
replace it with an inferred runtime weighting model.

## OMINO Character Matrix Tensor

The OMINO character matrix tensor is organized into 32-character cells across
the low and high planes.

```text
LOW PLANE
  0x(*/2)0 .. 0x(*/2 + 1)F
  32-character tensor window
  terminal control core anchors:
    0x(*/2 + 1)C .. 0x(*/2 + 1)F

HIGH PLANE
  low_coordinate ^ 0x80
  projected mirror window
  terminates at 0xFF full closure
```

The low plane is bounded to `0x00..0x7F`. The high plane is the exact XOR
reflection:

```text
high_coordinate = low_coordinate ^ 0x80
```

The tensor window is a coordinate view of the same canonical OMINO.

## Word-Atom Environmental Space

The 15 formal files are spatial environs around the fixed OMINO Centroid. They
do not define an ordinary storage system. Inside those environs, each word is an
autonomous atom with its own local slide-ruler basis.

```text
15 formal files
-> fixed OMINO Centroid, 0x00 / 0 degrees
-> autonomous omino word atoms
-> each word atom hosts 72 Circular Slide Rulers
-> each word atom hosts 4,320 local incidences
-> pairwise word interaction spans 4320^2 relational points
```

The 15-file environs matrix is:

```text
Face          .omi declarations       .imo definitions        .o executable
RULES         spatial law bounds      high-plane attributes   target register substrate
FACTS         ingest constants        input stream bytes      grounded hardware vectors
CLOSURES      closure thresholds      output coordinates      finite termination lanes
COMBINATORS   rotation shift maps     vector torque settings  axis clock drives
CONS          ordered pair templates  low/high plane maps     unified word axes
```

## Tri-Layer Blackboard Resolution

The Blackboard Pattern coordinates three domains of the same architecture:

```text
*.omi
  User-Local Resolution
  low plane ID boundary mapping
  nibble-interleaved form 0xY0X0

*.o
  Local-Binary Resolution
  real-time algorithmic register resolution

*.imo
  User-Remote Resolution
  high-plane data-omni boundary
  0xFFFFFFFF 0xFFFFFFFF
```

## COBS CONS 16x16 Blackboard Bounding Matrix

The COBS CONS Blackboard Pattern structures the full 256-byte space as one
16x16 byte board:

```text
row = high nibble
col = low nibble
```

The board is split by bit 7 into two macro-surfaces:

```text
Local Board Surface
  0x00..0x7F
  User-Local
  CAR / *.omi
  size: 0xFFFFFFFF

Remote Board Surface
  0x80..0xFF
  User-Remote
  CDR / *.imo
  size: 0xFFFFFFFF 0xFFFFFFFF
```

Canonical bounding rectangles:

```text
Local:
  (0x00, 0x07, 0x37, 0x30)
  (0x08, 0x0F, 0x3F, 0x38)
  (0x40, 0x47, 0x77, 0x70)
  (0x48, 0x4F, 0x7F, 0x78)

Remote:
  (0x80, 0x87, 0xB7, 0xB0)
  (0x88, 0x8F, 0xBF, 0xB8)
  (0xC0, 0xC7, 0xF7, 0xF0)
  (0xC8, 0xCF, 0xFF, 0xF8)
```

The target reduction is:

```text
CONS = 0xFFFFFFFF
```

Do not implement row or column traversal as nested loops inside the core.

## Lattice Retrieval Boundary

The high-dimensional lattice language is a retrieval boundary over fixed
coordinates, not a license for search loops.

```text
Leech Lattice Lambda_24
  24-ray boundary represented by 8 quadrants * 3 axes

II_25_1 Lorentzian horizon
  User-Remote 0xFFFFFFFF 0xFFFFFFFF high-plane boundary

Parabolic path metric
  Q60(x,y) = 4(4x^2 + 11x^2 + 4xy + y^2)
           = 4(11x^2 + (2x + y)^2)
```

Retrieval is constrained to branchless coordinate extraction, shift, rotate,
XOR, and bounded projection relative to the OMINO Centroid.

## Canonical [8,4,4] SECDED Streaming Cell

The previous physical rail-storage presentation:

```text
LOW  = 2 4 6 Delta
HIGH = 1 3 5 Delta

logical ring:
Delta 1 2 3 4 5 6 Delta
```

is superseded as the executable streaming-cell layout. It may remain as a
historical visualization of odd/even branch ordering only.

The streaming byte uses the canonical extended Hamming bit-vector map:

```text
Bit 0 -> LOGOS  -> parity check position P1
Bit 1 -> NOMOS  -> parity check position P2
Bit 2 -> FS     -> data position D1 -> FACTS
Bit 3 -> PATHOS -> parity check position P4
Bit 4 -> GS     -> data position D2 -> RULES
Bit 5 -> RS     -> data position D3 -> COMBINATORS
Bit 6 -> US     -> data position D4 -> CLOSURES
Bit 7 -> OMINO -> overall parity hinge -> CONS
```

The nested CONS streaming block is:

```text
Epistemic CONS:
  CAR(FS, GS, RS, US) . CDR(LOGOS, NOMOS, PATHOS)

Stream CONS:
  CAR(active run) . CDR(next run)
```

The bit-7 balance hinge and the centroid are two interpretations of the same
canonical OMINO, not two canonical names.

```text
OMINO =
  LOGOS ^ NOMOS ^ FS ^ PATHOS ^ GS ^ RS ^ US
```

The complete-cell mismatch is:

```text
overall_mismatch =
  LOGOS ^ NOMOS ^ FS ^ PATHOS ^ GS ^ RS ^ US ^ OMINO
```

Do not name this value `parity_valid` inside arithmetic kernels. The name
`overall_mismatch` preserves the exact algebra and prevents inverted outcome
logic.

## [SKILL_01: ENFORCE_CENTROID_METRIC]

```text
OBJECTIVE:
  Derive the next poly-omino state relative to the OMINO Centroid.

OWNER:
  OMINO Centroid
  Omnicron Runtime Resolver
  Gnomonic Projection Azimuth

INVARIANTS:
  Canonical Projective Reference == OMINO Centroid
  Pinch Point                   == 0x00
  Azimuth                       == 0 degrees
  Core Identity                 == Q60(x,y) = 4(4x^2 + 11x^2 + 4xy + y^2)

FORBIDDEN:
  software loops in the core transform
  variable address allocation as centroid motion
  floating-point offsets
  second centroid construction
  semantic branch selection

MANDATORY:
  branchless register transformations
  absolute distance from the fixed 0-degree azimuth
  one OMINO Centroid for every relative and antipodal orientation
```

```text
INPUT:
  current_omino_tile
  active_axis_context

TARGET:
  next_omino_tile

COMPUTE:
  left_torque  = ROL64(current_omino_tile, 1)
  right_torque = ROR64(active_axis_context, 2)
  blended      = left_torque ^ right_torque

  OUTPUT_omino_TILE = blended ^ 0x1D1D1D1D1D1D1D1DULL
```

## [SKILL_02: ENFORCE_GAUGE_0x18_WITNESS]

```text
OBJECTIVE:
  Enforce the 4! Gauge-Incidence Witness across the four low Omicron sectors.

OWNER:
  Metatron Incidence Scribe
  Omicron Gauge
  OMINO Centroid

INVARIANTS:
  4 Concentric Ring Sectors == 0x00..0x1F, 0x20..0x3F, 0x40..0x5F, 0x60..0x7F
  1 Sector Structure        == 28-position body + 4-position terminal hinge
  Canonical Core Target     == 0x18 == 24 decimal == 4!

FORBIDDEN:
  if, else, while in the core witness reduction
  dynamic loop iterators
  syntax parsing trees
  sector-local constants other than the canonical boundary form

MANDATORY:
  pure XOR boundary reduction
  high sector prefix cancellation
  fixed OMINO Centroid reference
```

```text
INPUT:
  sector_prefix

TARGET:
  gauge_incidence_witness

COMPUTE:
  body_start  = sector_prefix
  body_end    = sector_prefix | 0x1B
  hinge_start = sector_prefix | 0x1C
  hinge_end   = sector_prefix | 0x1F

  OUTPUT_WITNESS = body_start ^ body_end ^ hinge_start ^ hinge_end
```

The required result is:

```text
OUTPUT_WITNESS == 0x18
```

## [SKILL_03: EXECUTE_METATRON_SCRIBE]

```text
OBJECTIVE:
  Scribe place, polarity, incidence, and carry without semantic branches.

OWNER:
  Metatron Incidence Scribe
  omi-object-model

INVARIANTS:
  Baseline Witness == 0x18
  Shift Matrix     == 0x0001, 0x0010, 0x0100, 0x1000, 0x10000
  Target Surface   == omi-object-model 15 formal file strata

FORBIDDEN:
  if, else, switch in the core scribe calculation
  memory allocation buffers
  runtime interpreter queues
  meaning-derived instruction addresses

MANDATORY:
  fixed masks
  branchless equality masks
  direct bit alignment
  final 32-bit bounded output
```

```text
INPUT:
  inbound_relation_token
  current_scribe_register

TARGET:
  output_metatron_register

COMPUTE:
  hinge_bits = inbound_relation_token & 0x1F

  us_mask    = EQMASK32(hinge_bits, 0x1F) & 0x0001
  rs_mask    = EQMASK32(hinge_bits, 0x1E) & 0x0010
  gs_mask    = EQMASK32(hinge_bits, 0x1D) & 0x0100
  fs_mask    = EQMASK32(hinge_bits, 0x1B) & 0x1000
  carry_mask = NZMASK32(inbound_relation_token & 0xE0) & 0x10000

  scribe_step       = us_mask | rs_mask | gs_mask | fs_mask | carry_mask
  advanced_register = current_scribe_register ^ scribe_step ^ 0x18

  OUTPUT_METATRON_REG = advanced_register & 0xFFFFFFFF
```

`EQMASK32(a,b)` denotes a fixed-width all-ones mask when the two bounded values
match and zero otherwise. `NZMASK32(x)` denotes a fixed-width all-ones mask when
the bounded value is nonzero and zero otherwise. Implementations must lower
these to branchless fixed-width operations on the selected backend.

## [SKILL_04: RESOLVE_DOUBLE_EXPONENTIAL_HORIZON]

```text
OBJECTIVE:
  Extract six-variable logic outputs without CPU iteration loops.

OWNER:
  Omicron Gauge
  Omnicron Runtime Resolver

INVARIANTS:
  n = 6 variables       == 2^6 == 64 row combinations
  one uint64 word       == one complete selected 2^(2^6) logic universe
  Cross-axis intersect  == 4320^2
  Meta-matrix horizon   == 4320^(2^2)

FORBIDDEN:
  condition trees
  loop vectors
  dynamic array memory allocation
  sequential truth-table row tests

MANDATORY:
  six-bit row mask
  direct uint64 shift
  single-bit extraction
```

```text
INPUT:
  inbound_omino_word
  input_combination_6bit

TARGET:
  output_bit

COMPUTE:
  target_row = input_combination_6bit & 0x3F
  OUTPUT_BIT = (inbound_omino_word.table_universe >> target_row) & 0x01
```

## [SKILL_05: MAP_TRINOMIAL_WEIGHTS]

```text
OBJECTIVE:
  Apply Pascal Layer-4 multiplicity weights to the concentric slide rules.

OWNER:
  Gnomonic Projection Azimuth
  Algorithmic Clock

FORBIDDEN:
  inferred runtime weights
  probabilistic weighting
  loop-based coefficient search

MANDATORY:
  fixed 16-entry Layer-4 table
  nibble-bounded index
  coordinate metric relative to OMINO Centroid
```

```text
INPUT:
  quadrant_index_nibble
  coordinate_offset

TARGET:
  layer4_weight
  q16_output

COMPUTE:
  nibble        = quadrant_index_nibble & 0x0F
  layer4_weight = PASCAL_LAYER4[nibble]
  offset4       = coordinate_offset << 2
  Q16_OUTPUT    = offset4 * offset4
```

## [SKILL_06: BOUND_omino_CONTEXT]

```text
OBJECTIVE:
  Restrict the 4,320 local incidence grid to one omino tile.

OWNER:
  omino
  Omnicron Runtime Resolver

FORBIDDEN:
  loop vectors over 0..72
  incrementing counters as the model of the rulers
  index pointer shifting across separate objects
  treating 4,320 as program-memory capacity

MANDATORY:
  one fixed-width omino tile substrate
  18 Local240 Blackboard Faces
  4 Blackboard Quadrants per face
  60 Circular Ruler Positions per ruler
```

```text
INPUT:
  raw_target_address

TARGET:
  omino_tile_context

COMPUTE:
  local_face_count      = 18
  quadrant_count        = 4
  ruler_count           = local_face_count * quadrant_count
  circular_point_count  = 60
  local_incidence_count = ruler_count * circular_point_count
```

The required result is:

```text
ruler_count           == 72
local_incidence_count == 4320
```

The coordinate form is:

```text
R[b,q,phi]
```

where `b = 0..17`, `q = 0..3`, and `phi = 0..59`, all inside the
same omino tile context.

## [SKILL_07: ENFORCE_HAMMING_CELL]

```text
OBJECTIVE:
  Map the four Local240 quadrants to the systematic Hamming data positions.

OWNER:
  Omnicron Runtime Resolver
  Omicron Gauge

FORBIDDEN:
  string-category lookup
  documentation-folder routing
  semantic role transposition
  treating S-P-O-M as the primary owner of this map

MANDATORY:
  Bit_1 == FS == FACTS.omi
  Bit_2 == GS == RULES.omi
  Bit_3 == RS == COMBINATORS.omi
  Bit_4 == US == CLOSURES.omi
  CONS.omi folds the carried quartet against LOGOS/NOMOS/PATHOS
```

```text
INPUT:
  raw_7bit_cell
  current_word_state

TARGET:
  hamming_cell_routes

COMPUTE:
  message_bits = raw_7bit_cell & 0x0F

  FACTS_FS_GATE        = message_bits & 0x01
  RULES_GS_GATE        = message_bits & 0x02
  COMBINATORS_RS_GATE  = message_bits & 0x04
  CLOSURES_US_GATE     = message_bits & 0x08

  LOGOS  = FACTS_FS_GATE ^ RULES_GS_GATE ^ CLOSURES_US_GATE
  NOMOS  = FACTS_FS_GATE ^ COMBINATORS_RS_GATE ^ CLOSURES_US_GATE
  PATHOS = RULES_GS_GATE ^ COMBINATORS_RS_GATE ^ CLOSURES_US_GATE

  CONS_SURFACE = (FACTS RULES COMBINATORS CLOSURES . LOGOS NOMOS PATHOS)
```

This skill owns the five-file Hamming glossary. S-P-O-M remains a derived
projection after the Hamming cell is established.

## [SKILL_08: VERIFY_OMINO_TRILOGY]

```text
OBJECTIVE:
  Enforce the 15 formal files of the omi-object-model.

OWNER:
  omi-object-model
  omnicron-object-model

MANDATORY:
  strict axial-lisp dot-notation A-lists
  .omi declarations
  .imo definitions
  .o compiled immutable target substrate
```

```text
INPUT:
  input_stream_atom

TARGET:
  tri_layer_file_surface

COMPUTE:
  ROUTE rules facts closures combinations cons

  target.omi = CONS(RULES . COMBINATORS)
  target.imo = CONS(FACTS . CLOSURES)
  target.o   = CONDENSE(target.omi . target.imo)
```

The tri-layer surface is mandatory for compiler output and runtime exchange.
JSON, JSONL, XML, text logs, heap object trees, and tracking databases are not
native source or compiled-output layers. Conformance fixtures may exist outside
the compiler/runtime path, but they cannot redefine `.omi`, `.imo`, or `.o`.

## [SKILL_09: ROUTE_OMINO_TRILOGY]

```text
OBJECTIVE:
  Route the 15 formal files through the omi-object-model strata.

OWNER:
  omi-object-model
  omnicron-object-model

MANDATORY:
  rules
  facts
  closures
  combinations
  cons
  .omi
  .imo
  .o
```

```text
INPUT:
  input_meta_stream

TARGET:
  routed_formal_file_strata

COMPUTE:
  .omi declarations -> User-Local board layout from CONS
  .o binaries       -> Local-Binary register math
  .imo definitions  -> User-Remote blackboard states
```

## [SKILL_10: EXECUTE_CORE_TRANSFORM]

```text
OBJECTIVE:
  Advance coordinates without semantic jumps or instruction-pointer branches.

OWNER:
  Omnicron Runtime Resolver
  Algorithmic Clock
  Metatron Incidence Scribe

FORBIDDEN:
  if, else, switch, goto inside the core transform
  conditional jumps as semantic execution
  loop boundaries in the inner arithmetic kernel
  dynamic state mutation as the clock law

MANDATORY:
  XOR
  AND
  OR
  ROL
  ROR
  LSR
  SHL
```

```text
INPUT:
  current_coordinate_atom
  matrix_axis_context

TARGET:
  output_coordinate

COMPUTE:
  left_torque       = ROL64(current_coordinate_atom, 1)
  right_torque      = ROR64(matrix_axis_context, 2)
  blended_matrix    = left_torque ^ right_torque
  OUTPUT_COORDINATE = blended_matrix ^ 0x1D1D1D1D1D1D1D1DULL
```

## [SKILL_11: MAP_KLEIN_QUADRATIC]

```text
OBJECTIVE:
  Evaluate Q60(x,y) = 4(4x^2 + 11x^2 + 4xy + y^2) by direct shift-add math.

OWNER:
  Gnomonic Projection Azimuth
  OMINO Centroid

FORBIDDEN:
  algebraic approximation
  floating-point projection
  iterative metric refinement

MANDATORY:
  fixed-width integer arithmetic
  branchless term construction
  metric calculation relative to 0x00 / 0-degree OMINO Centroid
```

```text
INPUT:
  x_omi
  y_imo

TARGET:
  q60_metric

COMPUTE:
  x2        = x_omi * x_omi
  term_4x2  = x2 << 2
  term_11x2 = (x2 << 3) + (x2 << 1) + x2
  term_4xy  = (x_omi * y_imo) << 2
  term_y2   = y_imo * y_imo

  OUTPUT_METRIC = (term_4x2 + term_11x2 + term_4xy + term_y2) << 2
```

## [SKILL_12: RESOLVE_BLACKBOARD_STRATA]

```text
OBJECTIVE:
  Coordinate User-Local, Local-Binary, and User-Remote states branchlessly.

OWNER:
  omi-object-model
  omnicron-object-model

CONSTRAINTS:
  *.omi == User-Local Resolution  -> low plane ID boundary mapping 0xY0X0
  *.o   == Local-Binary Resolution -> real-time algorithmic register resolution
  *.imo == User-Remote Resolution -> high-plane boundary 0xFFFFFFFF 0xFFFFFFFF

FORBIDDEN:
  software evaluation loops
  memory state mutation pipelines
  data parsing buffers inside the core calculation

MANDATORY:
  static file stratum assignment
  branchless Local-Binary transform
  bounded high-plane complement projection
```

```text
INPUT:
  inbound_stream_atom
  current_matrix_axis
  local_frame_bounds

TARGET:
  blackboard_strata_coordinate

COMPUTE:
  local_frame      = local_frame_bounds & 0xFFFF
  stepped_register = ROL64(inbound_stream_atom, 1) ^ ROR64(current_matrix_axis, 2)
  local_binary     = stepped_register ^ 0x1D1D1D1D1D1D1D1DULL
  remote_hi        = (local_binary >> 32) ^ 0xFFFFFFFF
  remote_lo        = local_binary ^ 0xFFFFFFFF

  USER_LOCAL_omi   = local_frame
  LOCAL_BINARY_o   = local_binary
  USER_REMOTE_imo  = (remote_hi, remote_lo)
```

## [SKILL_13: RETRIEVE_LATTICE_COORDINATE]

```text
OBJECTIVE:
  Isolate high-dimensional lattice coordinates without sequential search.

OWNER:
  Gnomonic Projection Azimuth
  Omnicron Runtime Resolver
  OMINO Centroid

INVARIANTS:
  24 Leech Lattice rays == 8 quadrants * 3 axes
  II_25_1 null horizon  == 0xFFFFFFFF 0xFFFFFFFF User-Remote .imo matrix
  Parabolic path metric == Q60(x,y)

FORBIDDEN:
  sequential data searching
  thread looping
  dynamic array allocation
  nearest-neighbor scanning inside the core

MANDATORY:
  low-plane hinge extraction from .omi
  branchless barrel rotation over .o
  high-plane antipodal projection into .imo
```

```text
INPUT:
  input_stream_atom
  target_hyperbolic_axis
  low_plane_hinge

TARGET:
  lattice_coordinate

COMPUTE:
  local_boundary = low_plane_hinge & 0xFFFF
  torque_shift   = ROL64(input_stream_atom, 1) ^ ROR64(target_hyperbolic_axis, 2)
  local_binary   = torque_shift ^ 0x1D1D1D1D1D1D1D1DULL
  horizon_hi     = (local_binary >> 32) ^ 0xFFFFFFFF
  horizon_lo     = local_binary ^ 0xFFFFFFFF

  LATTICE_COORDINATE = (local_boundary, local_binary, horizon_hi, horizon_lo)
```

## [SKILL_14: RETRIEVE_HAMMING_STREAM_CELL]

```text
OBJECTIVE:
  Execute single-cycle [8,4,4] SECDED coordinate checks over the omino word.

OWNER:
  Omnicron Runtime Resolver
  Omicron Gauge
  Metatron Incidence Scribe

INVARIANTS:
  [7,4,3] block == single-error correction mode
  [8,4,4] block == single-error correction and double-error detection mode
  Epistemic CONS == CAR(FS, GS, RS, US) . CDR(LOGOS, NOMOS, PATHOS)

FORBIDDEN:
  if, else in the core stream cell calculation
  looping counters
  state variables as semantic storage
  sequential parity tracks

MANDATORY:
  exact bit-vector map
  branchless syndrome arithmetic
  bit-7 OMINO balance hinge bound to the OMINO Centroid
  overall_mismatch naming for complete-cell parity
```

```text
INPUT:
  raw_8bit_stream_word

TARGET:
  output_stream_cell

COMPUTE:
  LOGOS  = (raw_8bit_stream_word >> 0) & 1
  NOMOS  = (raw_8bit_stream_word >> 1) & 1
  FS     = (raw_8bit_stream_word >> 2) & 1
  PATHOS = (raw_8bit_stream_word >> 3) & 1
  GS     = (raw_8bit_stream_word >> 4) & 1
  RS     = (raw_8bit_stream_word >> 5) & 1
  US     = (raw_8bit_stream_word >> 6) & 1
  OMINO  = (raw_8bit_stream_word >> 7) & 1

  sLOGOS  = LOGOS ^ FS ^ GS ^ US
  sNOMOS  = NOMOS ^ FS ^ RS ^ US
  sPATHOS = PATHOS ^ GS ^ RS ^ US
  syndrome = sLOGOS | (sNOMOS << 1) | (sPATHOS << 2)

  computed_omino = LOGOS ^ NOMOS ^ FS ^ PATHOS ^ GS ^ RS ^ US
  overall_mismatch = OMINO ^ computed_omino

  interior_mask = NZMASK32(syndrome) & NZMASK32(overall_mismatch)
  omino_mask      = ZMASK32(syndrome) & NZMASK32(overall_mismatch)
  double_error    = NZMASK32(syndrome) & ZMASK32(overall_mismatch)
  correction_mask = SYNDROME_MASK8[syndrome]

  corrected_payload = raw_8bit_stream_word ^ (interior_mask & correction_mask)
  corrected_payload = corrected_payload ^ (omino_mask & 0x80)

  class0 = ZMASK32(syndrome) & ZMASK32(overall_mismatch)
  class1 = interior_mask
  class2 = omino_mask
  class3 = double_error
  result_code = PACK2(class1, class2, class3)

  OUTPUT_STREAM_CELL = (corrected_payload, syndrome, overall_mismatch, result_code)
```

`ZMASK32(x)`, `NZMASK32(x)`, and the syndrome correction selector denote
fixed-width masks that must lower to branchless backend operations. A backend
must define `SYNDROME_MASK8` as a fixed 8-entry table:

```text
SYNDROME_MASK8[0] = 0x00
SYNDROME_MASK8[1] = 0x01
SYNDROME_MASK8[2] = 0x02
SYNDROME_MASK8[3] = 0x04
SYNDROME_MASK8[4] = 0x08
SYNDROME_MASK8[5] = 0x10
SYNDROME_MASK8[6] = 0x20
SYNDROME_MASK8[7] = 0x40
```

The bounded result-code meanings are:

```text
00  no detected error
01  interior single error corrected
10  OMINO single error corrected
11  double error detected
```

## [SKILL_15: RETRIEVE_OMINO_TENSOR_CELL]

```text
OBJECTIVE:
  Extract a 32-character tensor coordinate from a single OMINO register context.

OWNER:
  OMINO
  Omicron Gauge
  Omnicron Runtime Resolver

INVARIANTS:
  Low Plane Bounds  == 0x(*/2)0 .. 0x(*/2 + 1)F, terminating at 0x7F
  Control Core      == 0x(*/2 + 1)C .. 0x(*/2 + 1)F
  High Plane Mirror == low coordinate ^ 0x80, terminating at 0xFF

FORBIDDEN:
  if, else in the core tensor extraction
  looping counters
  memory allocation blocks
  dynamic arrays

MANDATORY:
  7-bit low-plane mask
  0x80 high-plane reflection
  branchless coordinate transform
```

```text
INPUT:
  raw_tensor_address
  current_omino_word_state

TARGET:
  output_omino_cell

COMPUTE:
  low_coordinate  = raw_tensor_address & 0x7F
  high_coordinate = low_coordinate ^ 0x80

  left_torque  = ROL64(low_coordinate, 1)
  right_torque = ROR64(current_omino_word_state, 2)
  blended      = left_torque ^ right_torque

  OUTPUT_OMINO_CELL = blended ^ 0x1D1D1D1D1D1D1D1DULL
```

## [SKILL_16: RESOLVE_COBS_CONS_BOARD]

```text
OBJECTIVE:
  Route coordinates between Local and Remote board surfaces branchlessly.

OWNER:
  COBS CONS Blackboard Pattern
  Omnicron Runtime Resolver

INVARIANTS:
  Local Board CAR  == rows 0..7, columns 0..15, size 0xFFFFFFFF
  Remote Board CDR == rows 8..15, columns 0..15, size 0xFFFFFFFF 0xFFFFFFFF
  Target Reduction == CONS 0xFFFFFFFF

FORBIDDEN:
  if, else in the core board resolver
  nested coordinate loops
  array pointer index increments as the board law

MANDATORY:
  high-nibble row extraction
  low-nibble column extraction
  bit-7 Local/Remote split
  branchless CONS reduction
```

```text
INPUT:
  inbound_address_byte
  active_word_axis

TARGET:
  output_cons_word

COMPUTE:
  row = (inbound_address_byte >> 4) & 0x0F
  col = inbound_address_byte & 0x0F

  remote_cdr_bit  = (inbound_address_byte & 0x80) >> 7
  local_car_mask  = ZMASK32(remote_cdr_bit)
  remote_cdr_mask = NZMASK32(remote_cdr_bit)

  left_torque  = ROL64(inbound_address_byte, 1)
  right_torque = ROR64(active_word_axis, 2)
  blended      = left_torque ^ right_torque

  OUTPUT_CONS_WORD = (blended ^ 0x1D1D1D1D1D1D1D1DULL) & 0xFFFFFFFF
```

## [SKILL_17: ENFORCE_BOUNDED_COMPLEXITY_CLAIM]

```text
OBJECTIVE:
  Keep OMINO complexity claims bound to declared fixed-width coordinate surfaces.

OWNER:
  OMINO
  Omnicron Runtime Resolver

INVARIANTS:
  n = 6 local truth-table row boundary
  4320^2 cross-axis local coordinate horizon
  4320^(2^2) second-order local coordinate horizon
  no global unbounded P versus NP theorem claim

FORBIDDEN:
  claiming arbitrary unbounded P equals NP
  converting local O(1) register extraction into an infinity-scale theorem
  adding sequential row tests for fixed-width local retrieval

MANDATORY:
  local O(1) claim only for declared fixed-width surfaces
  explicit separation between local retrieval and unbounded complexity theory
  branchless bitwise register operations
```

```text
INPUT:
  table_universe
  input_combination_6bit

TARGET:
  bounded_local_output

COMPUTE:
  target_row = input_combination_6bit & 0x3F
  OUTPUT_BIT = (table_universe >> target_row) & 0x01
```

## [SKILL_18: ENFORCE_THREE_TIER_INCIDENCE]

```text
OBJECTIVE:
  Route the 15 canonical A-lists across the three local OMINO incidence surfaces.

OWNER:
  OMINO
  Omnicron Runtime Resolver
  COBS CONS Blackboard Pattern

INVARIANTS:
  Filespace Set      == rules, facts, closures, combinators, cons
  Compilation Strata == .omi User-Local, .imo User-Remote, .o Local-Binary
  Local Axis Metrics == 4320, 4320^2, 4320^4
  Klein Field        == QK(x,y) = 60x^2 + 16xy + 4y^2
  Cross Term Lock    == x = 15 -> 16xy = 240y -> 0 mod 240

FORBIDDEN:
  if, else in the core incidence decomposition
  runtime syntax parsing loops
  dynamic vector pointers
  database row traversal

MANDATORY:
  face partition from the 4-bit message frame
  three local surfaces derived from one bounded base coordinate
  Klein (60,15) cross-term neutralization
  branchless register transform over the COBS CONS 16x16 Blackboard Matrix
```

```text
INPUT:
  input_meta_stream_atom
  target_word_context

TARGET:
  output_omino_register

COMPUTE:
  face_bits = target_word_context & 0x0F

  base_index      = input_meta_stream_atom % 4320
  base_4320       = base_index
  surface_4320_2  = base_index * 4320
  horizon_4320_4  = base_index * 4320 * 4320 * 4320

  x = 15
  cross_term = (16 * x * base_index) % 240
  cross_term_zero = ZMASK32(cross_term)

  left_torque  = ROL64(input_meta_stream_atom, 1)
  right_torque = ROR64(target_word_context, 2)
  blended      = left_torque ^ right_torque

  OUTPUT_OMINO_REGISTER = blended ^ 0x1D1D1D1D1D1D1D1DULL
```

## [SKILL_19: DECOMPOSE_omino_SUBSTRATE]

```text
OBJECTIVE:
  Route incoming fields across the five core .o files and three local surfaces.

OWNER:
  OMINO
  omi-object-model
  Omnicron Runtime Resolver

INVARIANTS:
  Five Core Files == rules.o, facts.o, closures.o, combinators.o, cons.o
  Three Surfaces  == 4320, 4320^2, 4320^4
  Klein Field     == QK(x,y) = 60x^2 + 16xy + 4y^2
  Cross Term Lock == x = 15 -> 16xy = 240y -> 0 mod 240

FORBIDDEN:
  if, else in the substrate selector
  loop counters
  syntax parsers inside the core
  intermediate heap data arrays

MANDATORY:
  fixed five-entry substrate table
  vertex index bounded to 0..4
  three local surfaces derived from one base coordinate
  branchless XOR/shift/rotate transform
```

```text
INPUT:
  inbound_stream_token
  target_file_vertex
  axis_state

TARGET:
  output_register

COMPUTE:
  vertex = target_file_vertex & 0x07

  rules_mask       = EQMASK32(vertex, 0x00)
  facts_mask       = EQMASK32(vertex, 0x01)
  closures_mask    = EQMASK32(vertex, 0x02)
  combinators_mask = EQMASK32(vertex, 0x03)
  cons_mask        = EQMASK32(vertex, 0x04)

  substrate_id =
      (rules_mask       & 0x00)
    | (facts_mask       & 0x01)
    | (closures_mask    & 0x02)
    | (combinators_mask & 0x03)
    | (cons_mask        & 0x04)

  base_index      = inbound_stream_token % 4320
  base_4320       = base_index
  surface_4320_2  = base_index * 4320
  horizon_4320_4  = base_index * 4320 * 4320 * 4320

  x = 15
  cross_term = (16 * x * base_index) % 240
  cross_term_zero = ZMASK32(cross_term)

  left_torque  = ROL64(inbound_stream_token, 1)
  right_torque = ROR64(axis_state, 2)
  blended      = left_torque ^ right_torque

  OUTPUT_REGISTER = blended ^ 0x1D1D1D1D1D1D1D1DULL
```

## [SKILL_20: COMPILE_LOCAL_OMINO_FRAME]

```text
OBJECTIVE:
  Lower .omi and .imo streams into the five core .o local executable binaries.

OWNER:
  OMINO
  omi-object-model
  Omnicron Runtime Resolver

INVARIANTS:
  Target Outputs   == rules.o, facts.o, closures.o, combinators.o, cons.o
  Target Field     == 0x00..0x1F affine-control coordinate field
  Incidence Planes == 4320, 4320^2, 4320^4
  Klein Field      == QK(x,y) = 60x^2 + 16xy + 4y^2
  Cross Term Lock  == x = 15 -> 16xy = 240y -> 0 mod 240

FORBIDDEN:
  AST rewriting as the core compilation model
  linear memory loops
  dynamic pointer allocation
  nested tracking loops

MANDATORY:
  .omi User-Local input
  .imo User-Remote input
  five .o Local-Binary outputs
  branchless register transform
  O(1) coordinate derivation over the declared local frame
```

```text
INPUT:
  source_declaration_omi
  source_definition_imo
  active_axis

TARGET:
  output_omino_substrate

COMPUTE:
  declaration_base = source_declaration_omi % 4320
  definition_base  = source_definition_imo % 4320
  base_index       = declaration_base ^ definition_base

  base_4320      = base_index
  surface_4320_2 = base_index * 4320
  horizon_4320_4 = base_index * 4320 * 4320 * 4320

  x = 15
  cross_term = (16 * x * base_index) % 240
  cross_term_zero = ZMASK32(cross_term)

  left_torque  = ROL64(source_declaration_omi, 1)
  right_torque = ROR64(active_axis, 2)
  blended      = left_torque ^ right_torque

  OUTPUT_OMINO_SUBSTRATE = blended ^ 0x1D1D1D1D1D1D1D1DULL
```

## [SKILL_21: RESOLVE_REMOTE_INVARIANT]

```text
OBJECTIVE:
  Resolve remote streaming atoms through deterministic coordinate coherence.

OWNER:
  OMINO
  Omnicron Runtime Resolver
  Algorithmic Clock

INVARIANTS:
  SECDED Format    == [LOGOS NOMOS FS PATHOS GS RS US OMINO]
  Azimuth Origin   == OMINO Centroid, 0x00 / 0 degrees
  Master Clockwork == modulo-240 gear alignment
  Execution Basis  == Klein (60,15), with 16 * 15 * y == 240y == 0 mod 240
  Binary Surface   == rules.o, facts.o, closures.o, combinators.o, cons.o

FORBIDDEN:
  SHA or MD5 calculation loops inside the core resolver
  metadata headers as core authority
  network handshakes as core authority
  dynamic remote-state pipelines

MANDATORY:
  direct [8,4,4] bit extraction
  three local incidence surfaces
  branchless Algorithmic Clock torque
  five-file .o substrate output
```

```text
INPUT:
  inbound_remote_stream_byte
  active_local_axis

TARGET:
  output_embedded_register

COMPUTE:
  LOGOS  = (inbound_remote_stream_byte >> 0) & 1
  NOMOS  = (inbound_remote_stream_byte >> 1) & 1
  FS     = (inbound_remote_stream_byte >> 2) & 1
  PATHOS = (inbound_remote_stream_byte >> 3) & 1
  GS     = (inbound_remote_stream_byte >> 4) & 1
  RS     = (inbound_remote_stream_byte >> 5) & 1
  US     = (inbound_remote_stream_byte >> 6) & 1
  OMINO  = (inbound_remote_stream_byte >> 7) & 1

  base_index      = inbound_remote_stream_byte % 4320
  base_4320       = base_index
  surface_4320_2  = base_index * 4320
  horizon_4320_4  = base_index * 4320 * 4320 * 4320

  left_torque  = ROL64(inbound_remote_stream_byte, 1)
  right_torque = ROR64(active_local_axis, 2)
  blended      = left_torque ^ right_torque

  OUTPUT_EMBEDDED_REG = blended ^ 0x1D1D1D1D1D1D1D1DULL
```

## Final Lock

```text
OMI defines the Object Model.
OMINO is the poly-omino of the OMI Object Model.
The OMINO Centroid is fixed at 0x00 and 0 degrees.
Omnicron Object Model resolves its circular runtime relative to the OMINO Centroid.
The Algorithmic Clock advances the Circular Slide Ruler Array.
No form may become an additional source of core law.
```

## [SKILL_22: ENFORCE_PROOF_REPAIR_PLAN]

```text
OBJECTIVE:
  Keep the active Coq proof registry admit-free and OMINO-named.

ACTIVE OWNERS:
  /home/main/omi/omi-axioms/coq/00-foundations/OminoSECDEDCell.v
  /home/main/omi/omi-axioms/coq/02-closure/PowerClosureGate.v
  /home/main/omi/omi-axioms/coq/03-projection/E8RootsEnumerate240.v
  /home/main/omi/omi-axioms/coq/04-execution/OminoParallelSpatialScaling.v

INVARIANTS:
  OminoSECDEDCell.v maps [LOGOS NOMOS FS PATHOS GS RS US OMINO]
  PowerClosureGate.v proves the total numerator factorization
  E8RootsEnumerate240.v proves 112 + 128 = 240 and norm 2
  OminoParallelSpatialScaling.v proves constant step descriptor over a supplied substrate

FORBIDDEN:
  Admitted
  admit
  Axiom
  Parameter
  Conjecture
  Abort
  deprecated OMINO-name variants
  universal complexity claims not stated as supplied-substrate theorems

MANDATORY:
  make proof-registry-lock
  make proof-strict
  active 00..04 modules only for proof authority
  _archive/current remains quarantined until open obligations are discharged
```

## [SKILL_23: PROMOTE_AXIOMATIC_SUITE]

```text
OBJECTIVE:
  Maintain the 64-module independent active Coq registry.

COUNT LOCK:
  23 existing active modules
  + 38 independently promoted archive basenames
  + 3 active bridge modules
  = 64 active modules

FORBIDDEN:
  merging promoted archive basenames into existing owners
  collapsing promoted files into aggregate modules
  Admitted
  admit
  Axiom
  Parameter
  Conjecture
  Abort
  Omega or omega in active sources
  Fin.case_dep in active sources
  stale rational/natural vector drift in active sources

MANDATORY:
  every promoted archive basename remains an independent active file
  coq-docs/ARCHIVE-PROMOTION-MAP.md records the exact promotion map
  make proof-registry-lock
  make proof-strict
  proof-status must report active_sources=64
```
