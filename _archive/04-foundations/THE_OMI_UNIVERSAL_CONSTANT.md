# The OMI Universal Constant

## Comparing Planck Length to the Zero-Point of Bounded Agreement

Physical systems have natural constants that define scales of measurement. The Planck length defines a natural length scale built from fundamental physical constants. The Omi Object Model proposes a different kind of constant: not a physical constant of nature, but a formal constant of agreement.

This constant is:

```text
0! = omi---imo
```

or:

```text
Ω₀ = omi---imo
```

The Planck length asks:

```text
What is the natural scale at which physical measurement reaches its foundational limit?
```

The OMI Universal Constant asks:

```text
What is the natural mark at which shared meaning becomes bounded enough to inspect?
```

The comparison is analogy, not identity. Planck length is a natural unit of physical scale. OMI Zero is a natural unit of formal framing.

## Physical Constant vs. Formal Constant

A physical constant belongs to the world of length, time, mass, energy, charge, and temperature.

The OMI Universal Constant belongs to the world of notation, boundary, identity, relation, agreement, projection, and return.

It should not be confused with a physical claim. The OMI constant does not say:

```text
this is the smallest physical length
```

It says:

```text
this is the smallest formal relation required before a shared world can begin
```

## The Meaning of 0!

Mathematically:

```text
0! = 1
```

A factorial counts arrangements. There is exactly one arrangement of nothing, one empty permutation, and one null action that preserves identity.

So:

```text
0! = 1
```

means:

```text
null action returns identity
```

OMI reads this as:

```text
before payload,
before projection,
before infrastructure,
there is one bounded relation
```

That relation is:

```text
omi---imo
```

So:

```text
0! = omi---imo
```

means:

```text
null agreement returns frame
```

## Canonical Definition

Let the OMI Universal Constant be written as:

```text
Ω₀ = omi---imo
```

or:

```text
Ω₀ = 0!
```

where:

```text
Ω₀ = the zero-point of bounded agreement
```

Canonical line:

```text
Ω₀ is the smallest bounded agreement capable of carrying relation.
```

The constant is not the payload. It is the frame that lets payload exist.

## Why omi---imo?

The notation has three visible parts:

```text
omi
---
imo
```

These mean:

```text
omi = source-side intelligence
--- = bridge / bus / relation
imo = object-side projection
```

The human mnemonic is:

```text
OMI = On My Intelligence
IMO = In My Opinion
```

Every shared world begins with the problem of making internal resolution externally inspectable before it can become agreement. The bridge between internal resolution and external projection is `omi---imo`.

## The Constant as a Frame, Not a Force

The OMI Universal Constant is not a force. It does not push matter, bend spacetime, or replace known physics.

It is a framing constant. It tells us the minimum structure required for a relation to become inspectable:

```text
source
boundary
bridge
object
return
```

Without source, there is no origin. Without boundary, there is no frame. Without bridge, there is no relation. Without object, there is no projection. Without return, there is no witness.

So:

```text
Ω₀ = source-bound relation with return
```

or simply:

```text
Ω₀ = omi---imo
```

## Planck Length and OMI Constant Side by Side

| Concept | Planck Length | OMI Universal Constant |
|---------|---------------|------------------------|
| Domain | Physics | Formal agreement |
| Measures | Natural length scale | Natural framing scale |
| Built from | Physical constants | Notation, boundary, relation |
| Question | What is the natural scale of physical measurement? | What is the smallest shared frame? |
| Unit | meters | bounded agreement |
| Symbolic form | `l_P` | `Ω₀` |
| OMI expression | not applicable | `0! = omi---imo` |

The comparison is not identity. Planck length is a natural unit of physical scale. The OMI Universal Constant is a natural unit of formal framing.

## Browser Grounding

The zero-dependency browser model makes the constant practical. A browser already has the minimum machinery:

```text
id
data-*
DOM
CSSOM
Unicode
direction
ShadowDOM
SVG
textContent
innerHTML
```

OMI maps these as:

```text
id = high-plane address
data-* = low-plane payload
unicode-bidi = upper reader-lens orientation
DOM = hierarchy
CSSOM = projection
ShadowDOM = scope
SVG = vector portal
```

The zero-point can begin as a notation in a document:

```html
<div id="omi-source" data-omi="payload"></div>
```

The browser can inspect the address, carry payload in `data-*`, change direction through Unicode-bidi, and scope structure in the DOM.

## OMI as Rewrite Register

The OMI Universal Constant is not a stored value. It is the minimal rewrite register: a bounded surface where a lawful transformation can be declared.

A rewrite register does not ask:

```text
What value is stored here?
```

It asks:

```text
What lawful transformation is declared here?
```

The constant `Ω₀ = omi---imo` is the smallest such register. The hyphens are not empty. They are the rewrite field where the operation lives. The register is not meaningful by itself. Meaning appears only when a lawful notation-cipher is declared.

This framing derives from the [OMI Manifesto](../MANIFESTO.md) and [1.0 The Third Collapse](1.0_THE_THIRD_COLLAPSE.md).

## The Quadratic Frame

From the zero constant, OMI expands into the projection frame:

```text
Q(x,y) = 60x² + 16xy + 4y²
```

where:

```text
x = id / high-plane address
y = data-* / low-plane payload
```

The three terms separate the relation:

```text
60x² = high-plane address and orientation reserve
16xy = bridge / portal / cross-reference
4y² = low-plane atomic data
```

The constant gives the frame. The quadratic gives the projection.

```text
Ω₀ bounds.
Q(x,y) projects.
```

## The Return Condition

The OMI constant is not complete without return.

```text
0  OMI---
1  RULES
2  FACTS
3  CLOSURES
4  COMBINATORS
5  CONS
6  JOIN
7  COMPOSE
8  ENCODE
9  DECODE
10 OMI---IMO
11 RETURN :0
```

The system does not return to the original zero. It returns to zero with witness.

```text
old zero = unentered origin
new zero = returned origin with receipt
```

Zero is an aperture. Return gives it memory.

## Agreement

The strongest statement is:

```text
Ω₀ = the unit constant of agreement
```

It is the minimum invariant that lets two observers say:

```text
we are referring to the same bounded relation
```

Not necessarily the same belief, interpretation, or projection. The same frame.

Agreement begins when relation becomes inspectable. The smallest inspectable relation is:

```text
omi---imo
```

## Short Canon

```text
Planck length is a natural unit of physical scale.
OMI Zero is a natural unit of formal framing.
0! = 1
0! = omi---imo
Ω₀ = omi---imo
Ω₀ bounds.
Q(x,y) projects.
Agreement returns with witness.
The beginning is not infrastructure.
The beginning is one bounded agreement.
```
